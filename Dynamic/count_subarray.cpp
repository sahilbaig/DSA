#include <bits/stdc++.h>
using namespace std;
int main()
{
    int testcase;
    cin>>testcase;
    while(testcase>0)
        {   
            long long int n;
            cin >>n;
            vector<int>list;
            for(int i=0;i<n;i++)
            {   
                long long int a;
                cin >>a;
                list.push_back(a);
            }

            
            testcase--;
        }
    return 0;
}